import 'package:flutter/material.dart';

const BoxShadow kBoxShadow =
    BoxShadow(offset: Offset(0, 4), blurRadius: 15, color: Color(0x80000000));
const List<BoxShadow> kBoxShadowList = [kBoxShadow];

const darkColour = Color(0xff264653);
const mainColour = Color(0xff2a9d8f);
const highlightColour = Color(0xffe9c46a);
const accentColour = Color(0xfff4a261);
const accent2Colour = Color(0xffe76f51);
